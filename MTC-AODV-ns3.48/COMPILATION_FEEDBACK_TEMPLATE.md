# MTC-AODV Gate 1A — Compilation Feedback

Fill only the values produced by the tested computer. Use `N/A` when a command
could not be executed; never replace missing evidence with zero.

## Environment

```text
Operating system:
C++ compiler and version:
Python version:
ns-3 description (`git describe --always --dirty --tags`):
ns-3 root path:
```

## Build

```text
Command: ./ns3 configure --enable-examples --enable-tests
Exit code:
Result:
```

```text
Command: ./ns3 build
Exit code:
First compiler or linker error, if any:
```

Attach the complete file `mtcaodv-gate1-build.log`.

## Tests

```text
Suite: mtcaodv-attack-manager
Exit code:
PASS / FAIL / N/A:
```

```text
Suite: mtcaodv-blackhole-behavior
Exit code:
PASS / FAIL / N/A:
```

Attach both complete test logs when available.

## Python validation

```text
Command: python3 .../validate_gate1.py --ns3-root ...
Exit code:
PASS / FAIL / N/A:
```

Attach `mtcaodv-gate1-validation.json` when it was produced.

